# The shell runs offline on the demo stub. anthropic is OPTIONAL — only used when
# ANTHROPIC_API_KEY is set (e.g. for the optional bridge-prompt helper).
anthropic>=0.40.0

# Web layer: serves the frontend + /session (SSE) + /turn.
fastapi>=0.110.0
uvicorn>=0.29.0
